# ============================================================
#  KAIHARA OS — Python Dependencies
# ============================================================

# --- Core ---
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
tomli>=2.2.0;python_version<"3.11"

# --- LLM / Multi-Model ---
openai>=1.55.0          # OpenAI-compatible (works with Ollama too)
anthropic>=0.40.0
httpx>=0.28.0
tiktoken>=0.8.0

# --- Memory / Vector ---
chromadb>=0.5.20
rank-bm25>=0.2.2

# --- Agent Graphs (checkpointed) ---
langgraph>=0.2.50
langchain-core>=0.3.20

# --- MCP ---
mcp>=1.1.0

# --- Web / Scraping ---
playwright>=1.49.0
beautifulsoup4>=4.12.0

# --- Media / Image Processing ---
Pillow>=10.0.0           # Image generation, resize, filter, composite
MoviePy>=2.0.0           # Video slideshow with transitions
edge-tts>=6.1.0          # Microsoft Edge Neural TTS (BM voices)

# --- AI Image Generation (local) ---
diffusers>=0.30.0        # Stable Diffusion text-to-image
transformers>=4.40.0     # Required by diffusers
accelerate>=0.30.0       # Required by diffusers
torch>=2.0.0             # PyTorch backend

# --- Stock Media ---
# httpx already included above

# --- Voice (optional) ---
# openai-whisper>=20240930  # uncomment when enabling voice
# piper-tts                # local TTS fallback

# --- Channels ---
python-telegram-bot>=21.6
aiosmtplib>=3.0.0
# asyncio-imap — NOT on PyPI; stdlib imaplib used instead

# --- Security ---
cryptography>=43.0.0

# --- Monitoring / Utils ---
rich>=13.9.0
click>=8.1.0
PyYAML>=6.0.2
markdown>=3.7
